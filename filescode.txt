//2. Write a program to get employee roll number, name, department and salary for
//the desired number of employees and store the details in ‘Employee.dat’ file.
//Read the data from ‘Employee.dat’ file and calculate the annual income and
//print all the details in a neat form.
#include <iostream>
#include <fstream>
using namespace std;


int main(){
int noe,roll,salary;
string name,dept,emp;


cout<<"Enter the number of employees: ";
cin>>noe;
ofstream file;
ifstream file1;
file.open("Employeedetails.dat", ios::binary);
for(int i=0;i<noe;i++){
cout<<"Enter the roll number: ";
cin>>roll;
file.write((char *)&roll, sizeof(roll));
		cout<<"Enter the name: ";
cin>>name;
file.write((char *)&name, sizeof(name));
	cout<<endl;

cout<<"Enter the department: ";
cin>>dept;
file.write((char *)&dept, sizeof(dept));
	cout<<endl;
cout<<"Enter the salary: ";
cin>>salary;
file.write((char *)&salary, sizeof(salary));
//	cout<<endl;
}

file1.read((char *)&roll, sizeof(roll));
file1.read((char *)&name, sizeof(name));
file1.read((char *)&dept, sizeof(dept));
file1.read((char *)&salary, sizeof(salary));

cout<<"The roll number of the employee: "<<roll<<endl;
cout<<"Name of the employee: "<<endl;
cout<<"Department: "<<endl;
cout<<"Salary: "<<endl;
cout<<"Annua income: "<<(salary*12)<<endl;

file1.open("Employeedetails.dat",ios::in);
while(file1>>emp){
	cout<<emp<<" ";
}
file.close();
file1.close();
	return 0;
	}